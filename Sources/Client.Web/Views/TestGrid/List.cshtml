@using Client.Web.Controllers
@using MvcJqGrid
<div>
    @(Html.Grid("CustomerGrid")
    .SetCaption("UserRole")
    .AddColumn(new Column("Id").SetLabel("Id"))
    .AddColumn(new Column("Name"))
    .AddColumn(new Column("Version"))
    .SetUrl(Url.Action(TestGridController.ListAction, TestGridController.Name))
    .SetAutoWidth(true)
    .SetRowNum(10)
    .SetViewRecords(true)
    .SetPager("pager"))
</div>
